Build a release APK that includes the latest code changes using Android Studio.
Install the APK on my physical local device (Samsung Galaxy S24).
Use Android Studio for the build and installation.
If Android Studio cannot detect the device, use ADB to install the APK manually.
